<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <import resource="ldap.xml"/>
    <import resource="saml.xml"/>

    <security:authentication-manager alias="authenticationManager">
        <security:authentication-provider ref="ldapAuthenticationProvider" />
        <security:authentication-provider ref="samlAuthenticationProvider"/>
    </security:authentication-manager>

    <security:http disable-url-rewriting="true" use-expressions="true">
        <security:form-login login-page="/login" login-processing-url="/ldap/login" username-parameter="username" password-parameter="password" authentication-failure-url="/login?error=true" />
        <security:intercept-url pattern="/protected/**" access="isAuthenticated()" />
        <security:intercept-url pattern="/**" access="permitAll"/>
        <!--<security:custom-filter ref="promptFilter" after="SECURITY_CONTEXT_FILTER" />-->
        <security:logout logout-url="/logout" logout-success-url="/login?logout=true"/>
        <security:anonymous/>
    </security:http>

    <bean class="ch.wisv.dienst2.apiclient.util.Dienst2RestTemplateFactoryBean">
        <constructor-arg value="${dienst2.apitoken}" />
    </bean>

</beans>